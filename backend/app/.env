# Disable Mock Mode to connect to the real database
MOCK_MODE=False

# Connection URI (Local default is listed below; replace with Atlas URI if using cloud)
MONGODB_URI=mongodb://localhost:27017

# Name of the database to use
DATABASE_NAME=silvercare